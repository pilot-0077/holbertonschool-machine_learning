# Poisson Distribution

This project implements a `Poisson` class to model the Poisson distribution.

It allows initialization from raw data or a given `lambtha` value. It handles validation and computes the mean (`lambtha`) if data is provided.
